# XP Docker Environment

Docker configuration for the distributed **eXPerimental Project (XP)** platform.

This repository provides containerized infrastructure components required for development and testing.  
The initial stack includes a MariaDB instance with persistent storage and automated restart behavior.

---

## �� Getting Started

### �� Prerequisites

Make sure you have the following installed:

- Docker Engine  
- Docker Compose

Verify:

```bash
docker --version
docker compose version

